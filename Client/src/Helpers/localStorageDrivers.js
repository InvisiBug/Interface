export const localStorageParser = datapoint => {};

export const localStorageSaver = datapoint => {};

// export default thisone;

// import thisone, { localStorageParser, localStorageSaver } from "./path";
